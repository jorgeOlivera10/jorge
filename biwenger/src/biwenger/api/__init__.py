"""Capa de acceso a la API no oficial de Biwenger.

- endpoints.py: ÚNICO sitio con las URLs. Si la API cambia, se repara aquí.
- client.py:    cliente HTTP (login, token, cabeceras, reintentos, throttling, caché).
"""
